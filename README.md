# Nyanners

very basic c++ game engine that i'll probably never finish. loosely based on how roblox is built.

currently supports windows and linux, support for macOS never™️(compiles between linux / windows are a headache on their own already)
